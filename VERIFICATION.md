# Проверка сайта

Дата: 16.09.2026

- JavaScript: `node --check script.js` — без ошибок.
- Локальные ресурсы: отсутствующих файлов нет.
- Chromium, 1440 × 900: горизонтального переполнения нет, hero занимает экран, изображения загружены.
- Chromium, 1024 × 768: горизонтального переполнения нет, десктопная навигация корректно заменена кнопкой меню.
- Chromium, 390 × 844: горизонтального переполнения нет, одноколоночные услуги и проекты работают.
- Мобильное меню: открывается, блокирует прокрутку фона, закрывается.
- Галерея проекта: открывается, показывает счётчик и изображения.
- Предзагрузка: показывается до hero, анимирует новый знак и название «Денис Агафонов».
- Hero: три кадра автоматически сменяются; каждый начинает с увеличения и медленно отдаляется.
- Второй и третий hero-кадры заменены широкоформатными HQ-реконструкциями без растягивания небольших исходников.
- Липкая шапка: после прокрутки получает компактное затемнённое состояние.
- Счётчики: остаются на нуле во время прелоадера, запускаются через 450 мс после его ухода и завершаются значениями `2017`, `12`, `4`; год выводится без разделителя тысяч.
- Над липкой шапкой отображается золотая полоса прогресса прокрутки страницы.
- Пункт меню соответствующего текущему разделу подсвечивается золотым и получает нижнюю линию; активное состояние синхронизировано и с мобильным меню.
- Порядок пунктов меню соответствует последовательности разделов страницы: «О компании», «Преимущества», «Услуги», «Проекты», «Процесс».
- Телефон в шапке не имеет рамки или разделителя без наведения; при наведении затемняется фон, увеличивается номер и подсвечиваются трубка и подпись.
- Правый контактный блок и зона телефона имеют зарезервированную ширину, поэтому увеличение номера при наведении не сдвигает центральные пункты меню.
- Секция «Денис Агафонов» использует переданное широкое фото на всю ширину, зеркально отражённое CSS; человек расположен слева, текст — справа на полупрозрачной панели.
- Фото Дениса привязано к верхнему краю, а якорь учитывает высоту липкой шапки: голова остаётся видимой, возможная обрезка происходит снизу.
- В блоке преимуществ используются смысловые SVG-иконки: договор, гарантийный щит, команда и замер; номер расположен слева, иконка справа, включая мобильную версию.
- Favicon заменён на золотой фирменный знак «А» на тёмном фоне и явно подключён в `<head>`.
- Верхний и нижний логотипы ведут к якорю `#top` на начале документа, а не к фиксированной шапке.
- Анимации секций: элементы проявляются при входе в экран; учтён `prefers-reduced-motion`.
- Консоль браузера: ошибок и предупреждений нет.

Сайт локальный, публикация не выполнялась.
